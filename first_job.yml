


//First Workflow

name: First Workflow

on:
  - push

Jobs:
  First_job:
      runs-on: ubuntu-latest
      steps:
        - name: Welcome Message
          run: echo "My first GitHun Actions Job"

        - name: List Files
          run: ls
        
        - name: Read File
          run: cat dockerfile
